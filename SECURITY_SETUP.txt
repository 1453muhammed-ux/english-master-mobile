WORDPILOT v8.0.0 — GÜVENLİK DURUMU VE SONRAKİ ADIMLAR

MEVCUT DURUM
- Firebase App Check reCAPTCHA Enterprise site anahtarı ön yüzde kayıtlıdır.
- Firestore kuralları kullanıcı profili/alt ilerleme kayıtlarını yalnızca sahibine açar.
- Leaderboard okunması giriş gerektirir; kullanıcı yalnızca kendi sıralama satırını yazabilir.
- Feedback kayıtları alan ve uzunluk sınırlarıyla yalnızca giriş yapan kullanıcı tarafından oluşturulur.
- AI, voice ve protected-content kullanım sayaçları istemciye kapalıdır.
- Gizli OpenAI anahtarı ön yüzde bulunmaz.
- Hosting başlıklarında CSP, frame engeli, nosniff, referrer ve mikrofon izin politikası vardır.

V8 TESTER AYARLARI
- aiEnabled: false
- voiceTranscriptionEnabled: false
- protectedContentEnabled: false

Bu nedenle tester paketi Cloud Functions maliyeti oluşturmadan geliştirilmiş yerel Coach 3.0 ile çalışır. functions klasöründeki AI Coach ve transkripsiyon kodu, OpenAI Secret ve Functions dağıtımı tamamlanmadan canlı değildir.

ÖNEMLİ GERÇEK
Tarayıcıya gönderilen HTML, JavaScript, CSS veya statik JSON dosyaları F12 ile mutlak biçimde gizlenemez. Ticari içerik korumasının güçlü sürümü; giriş, App Check, sunucu uç noktası, hız sınırı ve küçük parçalı içerik teslimi gerektirir.

GELECEK CANLI AI SIRASI
1. Firebase Blaze planı ve bütçe uyarıları.
2. OPENAI_API_KEY değerinin Secret Manager'a eklenmesi.
3. Functions dağıtımı ve App Check enforcement kontrolü.
4. Kota/maliyet testleri.
5. security-config.js içinde aiEnabled:true.
6. Yalnızca kontrollü beta kullanıcılarıyla Cloud Coach testi.
